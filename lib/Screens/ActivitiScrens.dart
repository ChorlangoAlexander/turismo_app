import 'package:flutter/material.dart';

class ActivitiScrenns extends StatefulWidget {
  ActivitiScrenns({Key? key}) : super(key: key);

  @override
  State<ActivitiScrenns> createState() => _ActivitiScrennsState();
}

class _ActivitiScrennsState extends State<ActivitiScrenns> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Actividad')),
    );
  }
}
